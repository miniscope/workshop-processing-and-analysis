# Data: never commit recordings or intermediates — fetched by scripts/get_data.py.
data/sessions/
data/.cache/

# Minian dask scratch (intpath / MINIAN_INTERMEDIATE) — regenerated each run.
minian_intermediate/

# CaMAP capstone output bundles — written by ds.save_bundle(), regenerated each run.
capstone/output/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
build/
dist/

# Jupyter
.ipynb_checkpoints/

# Teaching notebooks (tutorials/<tool>/notebooks/) are now VENDORED and committed
# so they're present on clone — see tutorials/<tool>/notebooks/PROVENANCE.md.
# scripts/fetch_notebooks.py refreshes them (version-matched) but is optional.

# Secrets: archive API tokens (scripts/publish_*.py). Never commit these — a
# leaked token can rewrite or delete the lab's deposits. The .example files are
# the committed templates; the real ones are ignored.
.zenodo_token
.zenodo_token.sandbox
.figshare_token

# OS / editor
.DS_Store
.vscode/
.idea/
